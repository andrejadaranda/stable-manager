// Stub. Generate the real type with:
//   supabase gen types typescript --linked > lib/types/database.ts
// (or --project-id <ref>). Until then we use `any` so the supabase
// client compiles without surface-level type checking.
export type Database = any;
